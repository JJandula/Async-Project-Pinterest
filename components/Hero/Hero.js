import "./Hero.css"
import Cards from "../Cards/Cards";
import Input from "../Input/Input";

const Hero = () => `

<section class="hero">
    ${Input}
    <h2 class="explore-text">Explorar lo mejor de Pinterest</h2>
</section>

<section id="images-cont">
</section>

`

export default Hero
